# Phase 6 — 미구현 기능 전면 소화

작성 17:10 · **기능 동결 20:00** · 발표 **20:50**
A 전용 상세는 [`PHASE6_A_TASKS.md`](./PHASE6_A_TASKS.md)

---

## 1. 지금까지 요청됐는데 안 된 것 — 전수 조사

코드를 직접 확인했습니다. 추측이 아닙니다.

| # | 요청 | 상태 | 왜 안 됐나 |
|---|---|---|---|
| 1 | **구인자 공고 작성** | ❌ `EmployerJobsPlaceholder` | Tier 2로 계속 밀림 |
| 2 | **매칭 → 연결** | ⚠️ 알림만 뜸 | 눌러도 반응 없음. trigger·RPC는 준비 완료 |
| 3 | **사진 업로드** | ❌ | 내가 두 번 컷함(Storage 1.5인시) |
| 4 | **자기소개 1000자** | ❌ 아직 500 | A에게 넘겼는데 미반영 |
| 5 | **구인자→구직자 평점** | ❌ | Tier 2 미착수 |
| 6 | **공고 "원하는 성격"** | ❌ | Tier 2 미착수 |
| 7 | **인터랙티브 튜토리얼** | ❌ | 내가 컷함(앵커 추적 위험) |
| 8 | **실시간 채팅** | ❌ | 내가 컷함 |
| 9 | 회원탈퇴 | ❌ 신규 | |
| 10 | "사업자"→"구인자" | ❌ 신규 (20곳) | |

**3·7·8은 시간이 없어 내가 잘랐던 것**입니다. 마감이 20:50이라 다시 넣습니다.

---

## 2. 산수와 방침

남은 개발 시간 2시간 50분 × 2명 = **5.6인시**. 요청 총량 **약 8.5인시**.

사용자 결정: **"그냥 전부 다 해보자."**

그래서 자르지 않고 **가치 순으로 배열**합니다. 위에서부터 하고, 20:00에 있는 자리에서 멈춥니다.
**병렬성을 최대로 씁니다** — 오래 걸리는 것(튜토리얼 1.5h, 공고 작성 1.5h)을 가장 먼저 띄웁니다.

| 순위 | 항목 | 담당 | 시간 |
|---|---|---|---|
| 1 | 공고 작성 | A | 1.5h |
| 1 | 인터랙티브 튜토리얼 | B | 1.5h |
| 2 | "사업자"→"구인자" | B | 15분 |
| 2 | 자기소개 1000자 | A | 5분 |
| 3 | 매칭 연결 | B | 30분 |
| 4 | 회원탈퇴 | A | 40분 |
| 5 | 사진 업로드 | A 45분 + B 30분 | 1.25h |
| 6 | 평점 + 원하는 성격 | A 40분 + B 30분 | 1.2h |

채팅(#8)은 **여전히 안 합니다.** 실시간 채팅은 최소 2인시이고, 위 목록을 다 밀어내면서까지 넣을 가치가 없습니다. 매칭 연결(#2)이 같은 이야기를 완성합니다.

---

## 3. 🅱️ B 작업

### B-1 · "사업자" → "구인자" (15분)

소스 20곳. UI 문구만 바꾸고 **식별자는 그대로 둡니다** — `role === 'employer'`, `employer_id`, `useEmployerApplicants` 같은 코드 이름을 건드리면 A와 충돌하고 DB 컬럼과도 어긋납니다.

바꾸는 것: 화면에 보이는 한국어 "사업자" → "구인자"
안 바꾸는 것: `employer` 영문 식별자 전부, DB 컬럼, 파일명, 훅 이름

가입 역할 선택("알바생을 구하고 있어요")처럼 이미 자연스러운 문구는 그대로 둡니다.

### B-2 · 매칭 연결 (30분) — 알림이 지금 죽어 있음

"매칭됐어요!" 알림이 뜨는데 **누르면 아무 일도 없습니다.** A가 trigger와 `matched_contact()` RPC를 이미 만들어 뒀으니 연결만 하면 됩니다.

`src/features/notifications/MatchSheet.tsx`
- `type === 'mutual_match'` 알림 탭 → 시트
- `payload.counterpartId`로 `supabase.rpc('matched_contact', { counterpart })` 호출
  - **RPC 호출은 A 소유 경로가 필요합니다.** `src/hooks/useMatchedContact.ts`를 A에게 요청하거나, 없으면 B가 만들고 A에게 통보
- 내용:
  ```
  🎉 매칭됐어요!
  메가커피 안암역점 과 서로 관심이 있어요.

  이메일  owner@example.com        [복사]
             [ 메일 보내기 ]
  ```
- "메일 보내기"는 `mailto:` + 제목 미리 채움: `[AlbaSwipe] {가게명} 아르바이트 문의`
- 이메일이 `null`이면 "상대 정보를 불러올 수 없어요"
- `employer_interested` 알림은 시트 없이 지원 현황으로 이동

**이게 채팅 대신입니다.** 발표에서 "채팅은 다음 단계, 지금은 연락처를 교환합니다"로 설명합니다.

### B-3 · 인터랙티브 튜토리얼 (1.5h) — 가장 오래 걸림, 먼저 시작

지금은 정보 카드 4장입니다. 사용자 요청: **반투명 배경 + 실제 조작 유도.**

`src/features/onboarding/` 확장

- **스포트라이트**: 화면 전체를 `--color-backdrop`으로 덮되, 강조할 요소 위치만 뚫습니다.
  구현은 `box-shadow: 0 0 0 9999px var(--color-backdrop)`를 뚫을 영역에 거는 방식이 가장 안전합니다 — SVG mask나 clip-path보다 브라우저 호환이 넓고 깨질 일이 적습니다
- **앵커**: `data-tour="deck-card"` 같은 속성을 대상에 달고 `getBoundingClientRect()`로 위치를 잽니다
  - ⚠️ **스와이프 덱은 카드가 계속 바뀌어 앵커가 흔들립니다.** 덱 단계에서는 카드 개별이 아니라 **카드 스택 영역 전체**를 잡으세요. `ResizeObserver`로 위치를 갱신하고, 앵커를 못 찾으면 **스포트라이트 없이 설명만** 띄웁니다(절대 깨지면 안 됨)
- **단계**: ① 카드 소개 ② 오른쪽으로 밀어보기(실제 스와이프 대기) ③ 왼쪽으로 밀어보기 ④ 찜 탭 안내 ⑤ 완료
  - ②③은 **사용자가 실제로 스와이프할 때까지 기다립니다.** 일정 시간(8초) 안에 안 하면 "다음"으로 넘어갈 수 있게 버튼을 노출 — 막히면 안 됩니다
- 우상단 **X**로 언제든 종료, 하단 **다음**, 점 인디케이터 유지
- `localStorage` 키는 기존 `albaswipe.tutorial.seen` 재사용

**CRITICAL — 실패해도 앱이 멀쩡해야 합니다.** 튜토리얼은 첫 진입 화면입니다. 여기서 에러가 나면 데모 전체가 죽습니다. 튜토리얼 전체를 `ErrorBoundary`로 감싸고, 폴백은 **조용히 아무것도 렌더하지 않기**입니다.

### B-4 · 사진 업로드 UI (30분) — A의 훅 이후

`ProfileEditor`의 아바타 "준비 중" 자리를 실제 업로드로:
- `<input type="file" accept="image/*">`를 숨기고 아바타 탭으로 트리거
- 선택 즉시 `URL.createObjectURL`로 **미리보기**, 업로드 중 스피너
- 성공 토스트, 실패 시 에러 토스트(5MB 초과·이미지 아님은 A의 훅이 던집니다)
- 업로드된 아바타는 `ProfileAvatar`가 이미 `avatarUrl`을 받으므로 자동 반영

### B-5 · 평점 + 원하는 성격 UI (30분) — A의 스키마 이후

- `ApplicantDetail`에 별 5개 입력 + 평균 표시
- 지원자 카드에 평균 별점 작게 (공고 카드와 같은 모양)
- `BackFace`의 "근무 조건" 아래 **"이런 분을 찾아요"** 섹션 + `wantedTraits` 칩
- 내 프로필 성격과 겹치는 칩은 `bg-brand-soft text-brand`로 강조 (선택)

---

## 4. 🅰️ A 작업

[`PHASE6_A_TASKS.md`](./PHASE6_A_TASKS.md) 참조. 요약:

| | 항목 | 시간 |
|---|---|---|
| A-1 | 자기소개 1000자 | 5분 |
| A-2 | **공고 작성 폼 + INSERT 정책** | 1.5h |
| A-3 | 회원탈퇴 (`delete_own_account()` RPC) | 40분 |
| A-4 | 사진 업로드 Storage + `useAvatarUpload` | 45분 |
| A-5 | 평점 테이블 + `wanted_traits` 컬럼 | 40분 |

**A-2의 포맷 주의**: `work_days`는 `"월·수·금"`, `work_hours`는 `"13:00 ~ 18:00"`(공백 포함 ` ~ `). 시간 겹침 판정이 이 문자열을 파싱하므로 **틀리면 필터가 조용히 실패**합니다.

---

## 5. 타임라인

| 시각 | A | B |
|---|---|---|
| 17:10 | A-1 자기소개(5분) → **A-2 공고 작성** | **B-3 튜토리얼 착수** + B-1 용어 |
| 18:00 | A-2 계속 | B-2 매칭 연결 |
| 18:40 | A-2 마무리 → **A-4 Storage** 🔔 | B-3 계속 |
| 19:10 | A-3 회원탈퇴 | B-4 사진 UI |
| 19:40 | A-5 평점/성격 🔔 | B-5 평점 UI |
| **20:00** | **기능 동결** | **기능 동결** |
| 20:00~20:40 | 통합 · 배포 확인 · **실기기 리허설** | |
| 20:40 | 스와이프 기록 초기화 · 데모 계정 정리 | |

🔔 = 상대가 막혀 있으니 푸시 직후 알립니다(A-4는 B-4를, A-5는 B-5를 막습니다).

**19:00 체크포인트**: 여기서 안 끝난 항목은 버립니다. 새로 시작하지 않습니다.

---

## 6. 소유권 (신규)

| 경로 | 소유 |
|---|---|
| `src/features/onboarding/` (튜토리얼 확장) | B |
| `src/features/notifications/MatchSheet.tsx` | B |
| `src/pages/EmployerJobFormPage.tsx` · `src/hooks/useCreateJob.ts` | A |
| `src/hooks/useAvatarUpload.ts` · `useSeekerRating.ts` · `useMatchedContact.ts` | A |
| `src/features/profile/ProfileEditor.tsx` (사진 UI) | B |
| `src/features/employer-ui/` (평점 UI) | B |
| `src/features/wishlist/BackFace.tsx` (원하는 성격) | B |
| `src/types.ts` | 공동 — 단독 커밋 + `contract:` + 즉시 공지 |

---

## 7. 안 하는 것

**실시간 채팅.** 최소 2인시이고, 위 목록을 전부 밀어내면서까지 넣을 가치가 없습니다.
매칭 연결(B-2)이 "둘 다 관심 있으면 이어진다"는 이야기를 완성합니다. 발표에서 채팅은 다음 단계로 말하세요.
