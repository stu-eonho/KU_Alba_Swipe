# Phase 8 — 피드백 정리 + 미구현 전량 소화

작성 19:35 · **기능 동결 20:00** · 발표 **20:50**
현재 `main` = `213b334`, 라이브 번들 `index-DtN6Aw7d.js` (최신 반영 확인됨)

---

## 0. 먼저 — "공고 사진이 안 들어와 있다"는 건 이미 들어가 있습니다

확인했습니다. 배포된 번들 안에 문자열 `"공고 사진 추가"`가 **존재합니다.**

```
라이브 번들 = assets/index-DtN6Aw7d.js = 로컬 최신 빌드와 동일
grep "공고 사진 추가" → 1건
src/pages/EmployerJobFormPage.tsx:317  <JobImageField value={imageUrl} onChange={setImageUrl} />
```

즉 **코드는 배포돼 있고**, 보신 화면이 배포 직전 캐시였을 가능성이 높습니다.
위치는 공고 작성 폼의 **"상세 내용" 입력 바로 위**입니다.

> **먼저 하실 일:** 그 페이지에서 강력 새로고침(Ctrl+Shift+R / iOS는 탭 닫고 재접속) 후 다시 확인.
> 그래도 안 보이면 그때는 진짜 버그이므로 **F0으로 승격**합니다(아래 표에 자리만 잡아 둠).

---

## 1. 튜토리얼 — 증상 3개, 원인 2개

증상이 셋인데 원인은 둘입니다. 코드를 직접 읽고 확인했습니다.

### 원인 ① `startedOnDeck` — 설정에서 열면 무조건 옛날 튜토리얼

`src/features/onboarding/Tutorial.tsx`:

```ts
const [startedOnDeck] = useState(() => samePath(pathname, DECK_PATH[role] ?? DECK_PATH.seeker));
...
const useInteractive = TUTORIAL_INTERACTIVE && !interactiveFailed && startedOnDeck;
```

설정 화면에서 "튜토리얼 다시 보기"를 누르면 `pathname === '/settings'`입니다.
덱 경로(`/` 또는 `/employer/applicants`)가 아니므로 `startedOnDeck = false` →
**항상 슬라이드 폴백**(= 수정 전 정보 카드 4장)이 뜹니다.

이건 버그가 아니라 **의도된 폴백**이었습니다("덱이 없는 곳에서는 스포트라이트를 걸 데가 없다").
의도는 맞았지만 사용자가 원한 동작이 아닙니다. **폴백을 고치는 게 아니라 덱으로 데려가야 합니다.**

### 원인 ② 구인자 설정 화면에 메뉴 행 자체가 없음

`src/pages/SettingsPage.tsx:108`:

```tsx
{isSeeker ? (
  <> ... 내 정보 수정 / 지원 현황 / 가능한 시간 / 찜한 가게 /
      튜토리얼 다시 보기 / 스와이프 기록 초기화 ... </>
) : (
  <> 구인자용 짧은 목록 — 튜토리얼 행이 없음 </>
)}
```

"튜토리얼 다시 보기"가 **구직자 분기 안에만** 있습니다. 구인자는 누를 버튼이 없습니다.

### 세 번째 증상 "구인자 첫 진입 튜토리얼이 안 뜬다"

라우터 마운트는 `213b334`에 들어갔습니다(`MainLayout`의 구인자 분기).
따라서 **배포 시점 문제일 가능성이 높고**, 아니라면 남은 원인은 하나뿐입니다 —
`albaswipe.tutorial.seen.employer:<userId>` 가 이미 남아 있는 경우.

**검증 절차(추측 금지):**
1. 구인자 계정으로 로그인 → DevTools > Application > Local Storage
2. `albaswipe.tutorial.seen.employer:` 로 시작하는 키가 있는지 본다
3. 있으면 지우고 `/employer/applicants` 새로고침 → 뜨면 원인 확정(정상 동작)
4. 키가 없는데도 안 뜨면 그때는 `router.tsx` 마운트가 배포에 안 들어간 것

---

## 2. 작업 목록

**시각 기준 19:35, 동결 20:00 — 남은 개발 25분 × 2명 = 0.8인시.**
요청 총량은 그보다 큽니다. 그래서 **가치순으로 세우고 위에서부터** 합니다.
20:00에 있는 자리에서 멈추고, 그 아래는 발표에서 "다음 단계"로 말합니다.

| # | 항목 | 담당 | 시간 | 티어 |
|---|---|---|---|---|
| **G1** | 튜토리얼 3종 복구 | B | 20분 | **0** |
| **G2** | 지원 최소 20자 + 꼼수 차단 | B | 10분 | **0** |
| **G3** | 홈 "스와이프할 수 있는 공고 N개" | B | 10분 | **0** |
| **G4** | 홈 상단 아이콘 정리 (지역 · 가능시간) | B | 15분 | **0** |
| G5 | 수직 정렬 (여백 가운데) | B | 15분 | 1 |
| G6 | 템플릿 저장 미리보기 박스 | B | 15분 | 1 |
| G7 | 지역 필터 2단계 (시·도 → 구/시/군) | B | 30분 | 1 |
| G8 | 초기 취향 선택 (F1의 미구현 B분) | B | 25분 | 2 |
| F0 | 공고 사진이 정말 안 뜰 경우 | B | ? | 0 (조건부) |

A는 이 Phase에 **새 작업이 없습니다.** 전부 B 소유 화면입니다.
A는 **리허설·데이터 점검**을 맡습니다(§6).

---

## 3. 🅱️ G1 — 튜토리얼 3종 복구 (20분) · Tier 0

### G1-a. 설정에서 열면 덱으로 데려간다

`Tutorial`의 폴백 조건을 고치지 **않습니다.** 그 조건은 옳습니다 — 덱이 없는 화면에
스포트라이트를 걸면 앵커를 못 찾아 깨집니다. 대신 **호출부가 덱으로 이동한 뒤 엽니다.**

`src/pages/SettingsPage.tsx`:

```tsx
function replayTutorial() {
  resetTutorial(userId);
  // 튜토리얼은 덱 위에서만 인터랙티브로 돈다. 설정에서 바로 열면 옛날 슬라이드가 뜬다.
  // 그래서 덱으로 보내고, 거기 마운트된 <Tutorial /> 이 스스로 뜨게 둔다.
  navigate(user.role === 'employer' ? '/employer/applicants' : '/', { replace: false });
}
```

`setShowTutorial(true)`와 `<Tutorial open={showTutorial} .../>`는 **제거**합니다.
`resetTutorial()`이 두 역할 키를 모두 지우므로, 이동한 화면의 `<Tutorial />`이
`hasSeenTutorial === false`를 읽고 알아서 뜹니다. **제어 컴포넌트를 쓸 이유가 없습니다.**

⚠️ 이동 후 뜨지 않으면 원인은 하나입니다 — `Tutorial`의 `selfOpen`이
`useState(() => !hasSeenTutorial(...))`로 **마운트 시 한 번만** 읽습니다.
덱 화면이 이미 마운트돼 있었다면(탭 전환) 다시 읽지 않습니다.
`router.tsx`의 `<Tutorial key={user.id} .../>`에서 **key를 바꿔 강제 재마운트**하거나,
`Tutorial` 안에서 `location.key`를 의존성에 넣어 재평가하세요.
후자가 더 정확하지만 시간이 없으면 전자로 충분합니다.

### G1-b. 구인자 설정에 메뉴 행 추가

`SettingsPage`의 `isSeeker ? ... : ...` 분기에서, 구인자 목록에도
**"튜토리얼 다시 보기"**와 **"스와이프 기록 초기화"**를 넣습니다.
두 행은 역할과 무관하므로 **분기 밖 공통 구역으로 빼는 편**이 더 낫습니다 —
다음에 또 한쪽만 빠지는 사고를 막습니다.

### G1-c. 구인자 첫 진입 확인

§1의 검증 절차를 그대로 수행하고, 결과를 `_workspace/`에 적으세요.
**"안 된다"로 끝내지 말고 localStorage 키가 있었는지 없었는지를 남겨야** 다음 사람이 판단합니다.

---

## 4. 🅱️ G2 — 지원 최소 글자 수 (10분) · Tier 0

> "최소 20자 정도로. (띄어쓰기만 쓰다가 문자 하나 넣고 20자 채우는 기법 막아.)"

`src/features/apply/ApplyForm.tsx`. 지금은 길이 **상한만**(300) 있고 하한이 없습니다.

```ts
const MIN_MESSAGE_LEN = 20;

/**
 * 공백을 뺀 실제 글자 수. "   ㅁ   " 로 20자를 채우는 걸 막는다.
 * \s 만 지우면 유니코드 공백(U+3000 전각 스페이스)이 통과하므로 \p{White_Space} 를 쓴다.
 * 자모만 반복하는 것("ㅁㅁㅁ…")도 성의가 없지만, 정상 입력을 잘못 막는 쪽이 더 나쁘므로
 * 자모 검사까지는 하지 않는다. 공백 제거만으로 사용자가 말한 꼼수는 막힌다.
 */
function meaningfulLength(text: string): number {
  return text.replace(/\p{White_Space}/gu, '').length;
}
```

- 제출 버튼은 `meaningfulLength(message) < MIN_MESSAGE_LEN`이면 **비활성**
- 카운터를 `{value.length} / 300` → **미달일 때는 `20자 이상 (현재 N자)`** 로 바꿉니다.
  비활성 버튼만 두면 왜 안 눌리는지 알 수 없습니다
- 제출 직전에도 한 번 더 검사합니다(템플릿 삽입·붙여넣기로 상태가 어긋날 수 있음)

⚠️ **`u` 플래그가 필요합니다.** `\p{White_Space}`는 `/u` 없이는 문법 에러입니다.
⚠️ 이모지만 20개 넣는 건 통과합니다. 그건 막지 않습니다 — 정상 입력을 막는 위험이 더 큽니다.

---

## 5. 🅱️ G3~G8

### G3 · 홈 "스와이프할 수 있는 공고 N개" (10분) · Tier 0

`HomeDeckPage`에 이미 `visibleJobs`가 있습니다. 그 길이를 그대로 씁니다.

- 위치: 상단 아이콘 줄의 **왼쪽**(지금 비어 있는 자리)
- 문구: `스와이프할 수 있는 공고 {n}개`
- 12px `text-muted`. **레드 금지**
- `isLoading`·`isError`일 때는 그리지 않습니다(0개로 보이면 오해)
- 0개일 때도 그립니다 — 빈 상태 문구와 함께 있으면 이유가 분명해집니다

### G4 · 홈 상단 아이콘 정리 (15분) · Tier 0

> "해당 이미지가 너무 겹치는 느낌 남. 아이콘으로 대체 처리. 가능한 시간을 설정했으면
>  캘린더 모양으로, 전체 해제상태면, 캘린더에 작대기 그어서"

현재 상단에는 **지역 필터 아이콘 하나 + 텍스트 안내 줄**이 있어 세로로 두 줄을 먹습니다.
이를 **한 줄 · 아이콘 2개**로 줄입니다.

```
[스와이프할 수 있는 공고 12개]              [🗓 가능시간]  [⚙ 지역]
```

| 상태 | 아이콘 | 의미 |
|---|---|---|
| 가능 시간 등록됨 | `CalendarCheck` (lucide) | 딱 맞는 공고만 보는 중 |
| 미등록 / 전체 해제 | `CalendarOff` (lucide) | 시간 필터가 꺼져 있음 |

- 둘 다 `IconButton` + `aria-label`. 가능시간 아이콘은 `/settings/availability`로 이동
- **`aria-label`을 상태에 따라 바꿉니다** — `"가능한 시간 설정됨"` / `"가능한 시간 미설정"`.
  아이콘만 바꾸면 스크린리더에는 아무 변화가 없습니다
- 지역 필터 아이콘은 `SlidersHorizontal` → **`MapPin`**으로 교체합니다.
  사용자가 "겹친다"고 한 게 이 아이콘입니다. 지역 필터인데 일반 필터 모양이라 의미가 안 읽힙니다.
  선택된 지역 개수 배지는 유지
- 기존 `AvailabilityNotice`의 **텍스트 줄은 삭제하지 않습니다.** `hiddenCount > 0`일 때의
  "N개 숨김 / 전체 보기" 줄은 정보가 있고, 사용자가 지운 건 **미등록 안내 문구**입니다.
  → `!hasAvailability` 분기만 아이콘으로 대체하고, `hiddenCount > 0` 분기는 그대로 둡니다

### G5 · 수직 정렬 (15분) · Tier 1

> "주요 기능이 여백의 가운데가 아닌 약간 올라가있는 경향"

원인이 두 군데입니다.

**(1) 덱이 위에서부터 쌓입니다.** `CardStack`은 `flex flex-col items-center` + `mt-4`로
**상단 정렬**입니다. 남는 공간이 전부 아래에 몰립니다.

```tsx
// HomeDeckPage 바깥 컨테이너
<div className="flex min-h-[calc(100dvh-56px-64px)] flex-col overflow-hidden">
  ...상단 한 줄...
  <div className="flex flex-1 flex-col justify-center">  {/* ← 덱을 남은 공간 가운데로 */}
    <CardStack ... />
  </div>
</div>
```

**(2) 탭바가 있는 화면의 빈 상태가 `pt-16`으로 위에 붙어 있습니다.**
`HomeDeckPage`의 에러·빈 상태가 `flex flex-col items-center px-4 pt-16`입니다.
세로 가운데가 아니라 위에서 64px 지점에 고정돼 있어, 아래 여백이 훨씬 큽니다.
→ 바깥 컨테이너가 `flex-1`이 된 뒤에는 `pt-16`을 빼고 `justify-center`를 쓰면 됩니다.

**⚠️ `ApplyForm`·`JobDetailPage`의 `100dvh-56px`는 고치지 마세요.**
두 화면은 `FullscreenLayout` 아래라 **탭바가 없습니다**(`showTabBar={false}`).
64px를 빼면 오히려 어긋납니다. 확인하고 쓴 값입니다.

⚠️ `100vh` 금지 규칙이 그대로 적용됩니다. `100dvh`만 씁니다.

### G6 · 템플릿 저장 미리보기 (15분) · Tier 1

> "템플릿으로 저장 글자 아래에 네모 박스로, 글자수를 대략 100자정도 어떤 템플릿인지"

`ApplyForm`의 `ApplyMessageField`. **저장 버튼 아래**에 저장될 내용을 미리 보여 줍니다.

- 본문이 비어 있으면 박스를 **그리지 않습니다**
- `rounded-md border border-line bg-app p-3`, 12px `text-muted`, 그림자 없음
- 내용은 **앞 100자 + 넘치면 `…`**
- 위에 작은 라벨 `이렇게 저장돼요` (12px `text-faint`)
- 박스 안 줄바꿈은 `whitespace-pre-wrap`으로 살립니다. **`dangerouslySetInnerHTML` 금지**

이 박스는 저장 전에 **무엇이 저장되는지 보여주는 용도**이지, 저장된 템플릿 목록이 아닙니다.
목록은 위 칩 행이 이미 담당합니다.

### G7 · 지역 필터 2단계 (30분) · Tier 1

> "각 지역의 구(area)도 고를 수 있게. 뒤로가기 버튼 만들어서 city랑 area랑 왔다갔다"

**DB를 바꾸지 않습니다.** 주소에서 뽑을 수 있습니다 — 실제 데이터로 확인했습니다.

```
주소 "서울 서대문구 신촌로 83" → [0]="서울"(region, 이미 컬럼 있음) [1]="서대문구"(area)
```

현재 60건 기준 실제 분포:

| 시·도 | 구/시/군 |
|---|---|
| 서울 | 강남구 · 동대문구 · 마포구 · 서대문구 · 성북구 · 송파구 (6) |
| 경기 | 고양시 · 부천시 · 성남시 · 수원시 (4) |
| 인천 | 서구 · 연수구 · 중구 (3) |
| 부산 | 금정구 · 부산진구 · 해운대구 (3) |
| 대구/광주/대전/경북/경남/강원/충남 | 각 2 |
| 울산/충북/전남/전북/제주 | 각 1 |
| 세종 | `어진동` — **구가 없어 동이 나옵니다** |

**설계**

- `RegionFilterSheet`를 **2단계 뷰**로. 시트를 새로 만들지 말고 내부 상태만 추가합니다
  ```ts
  const [view, setView] = useState<{ mode: 'city' } | { mode: 'area'; city: string }>({ mode: 'city' });
  ```
- **city 뷰**: 지금과 동일 + 각 칩 우측에 `ChevronRight`. 칩 본체 탭 = 그 시·도 전체 선택,
  화살표 탭 = area 뷰로 진입. **두 타겟을 겹치지 마세요** — 하나의 버튼 안에 버튼을 넣으면
  HTML이 무효이고 클릭이 엉킵니다. `<div role="group">` 안에 버튼 2개로 나눕니다
- **area 뷰**: 헤더 좌측 `ChevronLeft` 뒤로가기(→ city 뷰) + 제목에 시·도 이름,
  본문은 그 시·도의 구 목록 + 맨 위 `{시·도} 전체` 칩
- **Escape·백드롭은 시트 자체를 닫습니다.** area 뷰에서 Escape가 city 뷰로만 가면
  사용자가 시트를 못 닫습니다. 뒤로가기는 **버튼으로만**
- 저장 형식은 `regionStorage`를 확장합니다. **기존 키를 깨지 마세요** —
  `albaswipe.regions`에 문자열 배열이 이미 저장돼 있습니다
  ```ts
  // 시·도만 고르면 "서울", 구까지 고르면 "서울>서대문구"
  // 읽을 때 '>' 가 없으면 예전 값이므로 시·도 전체로 해석한다 — 마이그레이션 불필요
  ```
- **세종은 area 단계를 만들지 않습니다.** 동 단위가 나와 의미가 없습니다. 칩에 화살표를 숨기세요
- 필터링은 `HomeDeckPage`에서 합니다. `useDeck`은 **시·도만** 받습니다(A 소유, 시그니처 유지).
  구 단위는 화면에서 `job.address.split(' ')[1]`로 한 번 더 거릅니다
- `regionCount`(A가 주는 시·도 기준 건수)와 구 필터 결과가 다를 수 있습니다.
  **빈 상태 판단은 구 필터까지 적용한 결과로** 하세요

### G8 · 초기 취향 선택 (25분) · Tier 2 — F1의 미구현분

A가 `recommend.ts` · `usePreferences` · `user_preferences` 테이블을 다 만들었지만
**B쪽 화면이 없습니다.** 지금 추천은 스와이프를 쌓아야만 동작합니다.

- 첫 진입(취향 가중치가 비었을 때) 홈 상단에 **한 줄 배너**:
  `관심 업종을 고르면 더 잘 맞는 공고부터 보여드려요  [고르기]`
- 탭하면 업종 칩 다중 선택 시트(`JOB_CATEGORIES` 11종 재사용).
  고른 업종에 초기 가중치를 넣습니다 — `usePreferences`의 공개 API를 먼저 읽고 맞추세요
- 배너는 **가중치가 생기면 사라집니다**. 닫기(X)도 주고 `localStorage`로 기억합니다
- 이건 **없어도 앱이 도는 기능**입니다. 20:00이 가까우면 버립니다

---

## 6. 🅰️ A 작업 — 새 기능 없음, 검증만

1. **데이터 점검**
   ```sql
   select count(*) from jobs;                                    -- 60
   select count(*) from jobs where region is null or region = ''; -- 0
   select count(*) from jobs
     where work_hours !~ '^[0-9]{2}:[0-9]{2} ~ [0-9]{2}:[0-9]{2}$';  -- 0
   select count(*) from jobs where split_part(address,' ',2) = '';   -- 0  ← G7이 이걸 씁니다
   ```
   마지막 줄이 0이 아니면 **G7이 그 공고에서 조용히 실패**합니다. B에게 즉시 알리세요.
2. **구인자 데모 계정**으로 공고를 하나 실제로 작성해 보고, 사진 첨부가 되는지 확인
3. `job-images` 버킷에 파일이 올라가는지 (RLS 정책이 `auth.uid()` 폴더를 요구합니다)
4. 20:20 이후 **스와이프 기록 초기화** — 데모에서 덱이 비어 있으면 안 됩니다

---

## 7. 타임라인

| 시각 | B | A |
|---|---|---|
| 19:35 | **G1 튜토리얼** | 데이터 점검 SQL |
| 19:55 | G2 최소 글자 · G3 공고 수 | 구인자 공고 작성 실기기 확인 |
| 20:00 | **기능 동결 — G4까지만** | |
| 20:00~20:30 | 통합 · 빌드 · 배포 · 실기기 리허설 | 동일 |
| 20:30 | 스와이프 기록 초기화 · 데모 계정 정리 | |

**G5~G8은 20:00 전에 끝나지 않으면 들어가지 않습니다.**
Tier 0(G1~G4)이 사용자가 "안 된다"고 말한 것들이고, 나머지는 "있으면 좋겠다"입니다.
안 되는 걸 먼저 고칩니다.

---

## 8. 소유권

| 경로 | 소유 |
|---|---|
| `src/pages/SettingsPage.tsx` | B (Phase 3에서 A→B 이관, `9724f36`) |
| `src/pages/HomeDeckPage.tsx` · `src/features/deck/` | B |
| `src/features/apply/` · `src/features/onboarding/` | B |
| `src/components/layout/` · `src/styles/` · `src/router.tsx` | B |
| `src/lib/` · `src/hooks/` · `src/types.ts` · `supabase/` | **A — 읽기만** |

**이번 Phase에서 B가 A 파일을 고칠 일은 없습니다.** 필요해지면 고치지 말고 기록 후 알리세요.

---

## 9. 안 하는 것

**실시간 채팅.** 최소 2인시입니다. 남은 시간 전체보다 큽니다.
매칭되면 `MatchSheet`가 이메일을 교환하고 `mailto:`를 엽니다 —
발표에서는 **"매칭까지가 제품이고, 채팅은 다음 단계"**로 말하세요.
그게 변명이 아니라 실제로 이 앱이 푸는 문제(서로 관심 있는 상대를 찾는 것)의 끝입니다.

---

## 10. 검증

머지 조건은 그대로입니다.

```bash
bash .claude/skills/ownership-verify/scripts/verify.sh   # 8개 항목 PASS
npm run build
```

추가로 **G1은 빌드로 검증되지 않습니다.** 반드시 손으로 확인하세요:

- [ ] 구직자 설정 > 튜토리얼 다시 보기 → **스포트라이트** 튜토리얼(슬라이드 아님)
- [ ] 구인자 설정에 **"튜토리얼 다시 보기" 행이 보인다**
- [ ] 구인자로 눌렀을 때 `/employer/applicants`로 이동하며 튜토리얼이 뜬다
- [ ] 지원 화면에서 공백 20자로는 **제출 버튼이 안 눌린다**
- [ ] 홈 상단에 공고 수와 아이콘 2개가 **한 줄에** 있다
