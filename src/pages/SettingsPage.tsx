/**
 * OWNER: Dev A (data/auth) — TODO(A): See SPEC <settings_view>.
 * Build "스와이프 기록 초기화" FIRST. Without it every rehearsal needs a new account.
 */
export default function SettingsPage() {
  return <div>TODO(A): SettingsPage</div>;
}
