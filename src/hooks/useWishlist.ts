/**
 * OWNER: Dev A (data/auth)
 *
 * queryKey: ['swipes'] - invalidate after createSwipe and after unwishlist.
 *
 * TODO(A): return { entries, isLoading, isError, remove(jobId) }
 */
export function useWishlist() {
  throw new Error('TODO(A): not implemented');
}
