/**
 * OWNER: Dev A (data/auth)
 *
 * queryKey: ['reviews', jobId] - staleTime Infinity (immutable within a session).
 *
 * TODO(A): return { reviews, isLoading }
 */
export function useReviews(_jobId: string) {
  throw new Error('TODO(A): not implemented');
}
