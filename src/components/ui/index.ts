/**
 * OWNER: Dev B (screens) — SOLE OWNER
 *
 * Shared primitives live here. Dev A imports from "@/components/ui" and never
 * creates their own Button/Input. Two sets of primitives means a broken-looking app.
 *
 * TODO(B), in this order (Dev A is blocked on the first five):
 *   Button, Input, Chip, Badge, EmptyState, Skeleton, Spinner, IconButton
 */
export {};
